---
description: Отревьюить текущие изменения — логика, безопасность, edge-cases (агент reviewer)
---

Запусти `reviewer` по изменениям, которые уже внесены в рабочее дерево.

Что смотреть особенно внимательно (необязательно): $ARGUMENTS

## Правила

- `reviewer` собирает диф сам (`git status`, `git diff`). **Проект не под git** — он ревьюит
  по списку файлов из `PLAN.md` папки задачи и отчёта `implementer`; проследи, чтобы он не
  поставил `verdict: approved` вслепую.
- Папку задачи `reviewer` берёт сам — `node .claude/hooks/task.mjs path` (Bash у него есть).
  Активной задачи нет — он обязан остановиться, а не писать ревью «куда-нибудь».
- Замечания уходят в `REVIEW.md` **этой же папки**. Вердикт стоит в шапке файла:
  `verdict: approved | changes_requested | blocked`.
- Код `reviewer` **не правит**. Это **одиночный запуск, петли доработки здесь нет** — она
  работает в `/cckit_feature` (режимы A/B). Вернуть замечания в работу решаешь ты:
  `/cckit_implement` — правки по `REVIEW.md` в рамках действующего плана; `/cckit_plan …` —
  если замечание меняет сам замысел.
- Отдай мне выжимку: сколько замечаний по категориям, самые важные, и ручной сценарий проверки.
