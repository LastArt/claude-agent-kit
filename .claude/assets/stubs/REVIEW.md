---
verdict: <approved | changes_requested | blocked>
critical: 0
important: 0
minor: 0
---

# REVIEW — <название задачи>

> Файл ведёт агент **reviewer**. Код не правит — только фиксирует замечания.
> Замечаний нет — ставится `verdict: approved` и нули в счётчиках, **а файл остаётся целиком**:
> шапка, соответствие плану, ручной сценарий. Раньше вместо всего файла писалось одно слово
> «одобрено» латиницей; оно ушло, потому что по нему нельзя было отличить «одобрено»
> от «ревью провести невозможно».

**Шапка выше — вход управляющей логики, а не оформление.** По ней петля доработки решает,
вернуть ли задачу исполнителю, поэтому формат жёсткий:

- `---` стоит **первой строкой файла**, выше неё нет ничего — ни пустой строки, ни заголовка;
- блок в файле **ровно один**; `verdict:` где-то ниже закрывающего `---` управляющей логикой
  игнорируется;
- полей четыре: `verdict`, `critical`, `important`, `minor`. Числа обязаны сходиться
  с таблицами замечаний ниже.

**Три вердикта:**

- `approved` — замечаний нет, счётчики нулевые;
- `changes_requested` — есть что исправить. **Любая** тяжесть находок даёт именно его: тяжесть
  выражает шкала 🔴 / 🟡 / ⚪ в таблицах, а не вердикт;
- `blocked` — **ревью невозможно**: нет плана или он нечитаем, предмет ревью не определить.
  Это **не** «нашлось страшное» — страшное всегда `changes_requested`. И это ни статус задачи
  `blocked` (задача остановлена, дальше решает человек), ни остановленная машинная приёмка
  (`GATE_STATE.json`).

**Дата:** _(YYYY-MM-DD)_
**Что ревьюилось:** _(git diff по каким файлам)_
**План:** `PLAN.md` — в этой же папке задачи

---

## 🔴 Критично (блокирует)

| # | Файл:строка | Проблема | Как исправить |
|---|-------------|----------|---------------|
| | | | |

## 🟡 Важно (исправить до релиза)

| # | Файл:строка | Проблема | Как исправить |
|---|-------------|----------|---------------|
| | | | |

## ⚪ Мелочи (на усмотрение)

| # | Файл:строка | Замечание |
|---|-------------|-----------|
| | | |

---

## Безопасность

> Блок **обязателен всегда**, включая пустой: тогда в нём одна строка — что проверял и чем.
> Шкала та же, 🔴 / 🟡 / ⚪, и находки отсюда считаются в те же счётчики шапки.

Содержание блока зависит от класса риска задачи, а класс берётся из поля **`class`
в `STATE.md`** (не из `PLAN.md`: в `STATE.md` лежит итог с уже применённым машинным полом):

- `elevated` — стенд обязателен, разбор по коду обязателен;
- `standard` — стенд по возможности, разбор по коду обязателен;
- `cosmetic` — одна строка «изменения не затрагивают ввод, права и данные».

Значения секретов, токенов и персональных данных, добытые на стенде, в этот блок
**не переносятся** — они описываются словами: `REVIEW.md` вшивается в `map.html`, а карту
пересылают.

| # | Файл:строка | Находка | Тяжесть | Как исправить |
|---|-------------|---------|---------|---------------|
| | | | | |

---

## Соответствие плану

- Шаги плана выполнены: _(да / нет — какие расхождения)_
- Изменения вне плана: _(нет / список файлов)_

## Что проверить на сервере / командами

- _(команды из PROJECT_PROFILE.md: тесты/линт/сборка/деплой)_

## Ручной сценарий проверки

- _(пронумерованные шаги; под какой ролью; какие edge-cases; замена автотестам, если их нет)_

## Итерации

> Заполняется со второй итерации ревью. Ответы исполнителя переносит сюда **ревьюер**: файл он
> перезаписывает целиком, и без переноса история несогласий стиралась бы каждый круг молча.
> Сам исполнитель правит в этом файле **только** этот раздел.

| # | Замечание | Ответ исполнителя (исправлено / не согласен: довод) | Закрыто |
|---|-----------|------------------------------------------------------|---------|
| | | | |
