# DONE — <название задачи>

> Файл пишет **оркестратор** по завершении ревью. Не ревьюер — его ход к этому моменту
> закончен, а вердикт уже прочитан; не исполнитель — отчёт о своей работе, написанный тем,
> кто её делал, теряет смысл; оркестратор единственный видит цепочку целиком. Техническая
> возможность записать этот файл есть и у ревьюера (у него есть `Bash`) — это соглашение
> о ролях, а не граница прав.
>
> Задача при этом переходит в `awaiting_acceptance`, конвейер сессию не держит; взвод гейта
> снимается, если машинная приёмка это позволяет, а если нет — файл написан, но приёмка
> не снята, и об этом сказано строкой.

**Дата:** _(YYYY-MM-DD)_

---

## Что сделано

_(один абзац: что работает после задачи, чего не работало до)_

## Затронутые файлы

- `путь/к/файлу` — _(что изменилось)_

## Вердикт ревью и безопасность

_(вердикт из шапки `REVIEW.md` плюс одна-две строки «что нашли и чем закрыли». Находки
дословно сюда **не переносятся** — они остаются в `REVIEW.md` и `SECURITY.md`: этот файл
вшивается в `map.html`, дубль расширяет поверхность и расходится при первой же правке.)_

## Проверить руками до слияния

- [ ] _(что человеку посмотреть своими глазами: экран, команда, сценарий)_
- [ ] _(что не проверяется машинно и видно только в живой системе)_

## Осталось незакрытым

- _(что и почему: отложенный риск, ручное действие, отдельная задача)_
